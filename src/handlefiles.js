/* - Handlefiles -
 * A very easy way to handle multiple file/template loading
 * in nodejs, combining them, and parsing them with one
 * object.
 * @author boboman13
 */

module.exports = {
	parseFiles: require('./parsefiles')
};